# ============================================================
#  backend 폴더 requirements.txt
#  (Docker build → push → GCP Cloud Run 배포용)
# ============================================================
#  참고: sqlite3 는 파이썬 표준 라이브러리이므로 설치 목록에 없습니다.
#        (우리 DB 계층은 sqlite3 만 사용, SQLAlchemy 미사용)

# ── FastAPI 백엔드 필수 ──────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2

# ── (심화) 리뷰 감성 분석 모델용 ─────────────────────────
#  한국어 감성 분류(KcELECTRA 계열) 추론에 사용합니다.
#  CPU 환경(Cloud Run)에서 동작하도록 아래 버전을 사용합니다.
#  ※ 저사양/오프라인 환경이라면 이 3줄을 제거하고
#    환경변수 USE_ML_MODEL=0 으로 실행하면 규칙 기반으로만 동작합니다.
transformers==4.44.2
torch==2.4.1
sentencepiece==0.2.0
